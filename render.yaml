services:
  - type: web
    name: the-random-crap
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: thoughts-data
      mountPath: /data
      sizeGB: 1